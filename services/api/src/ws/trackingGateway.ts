// Phase 2 (Module 1 — Lorry rental end-to-end) adds the live tracking
// gateway here: a Socket.io room per trip_id, driver location pings
// broadcast to subscribed customers/owners, and debounced ETA recalculation
// via Google Directions. Left as a placeholder so the directory structure
// matches the phased build plan from Phase 0 onward.
export {};
